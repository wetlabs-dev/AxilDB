import { prisma } from '@/lib/prisma'
import { createPropagationEvent, deletePropagationEvent } from '@/app/actions'
import { Card, Field, TextArea, Button } from '@/components/ui'
import { ConfirmDeleteButton } from '@/components/ConfirmDeleteButton'
import { canCreate, getCurrentUser, isAdmin } from '@/lib/auth'
import { fmtDate, plantName } from '@/lib/utils'
import Link from 'next/link'

export default async function Propagations(){
 const user=await getCurrentUser()
 const [instances,events]=await Promise.all([prisma.plantInstance.findMany({where:{status:'ACTIVE'},include:{plantDefinition:true},orderBy:{plantId:'asc'}}),prisma.propagationEvent.findMany({include:{parents:{include:{parentPlantInstance:true}},children:{include:{childPlantInstance:true}}},orderBy:{date:'desc'}})])
 return <div className='space-y-6'><h2 className='text-3xl font-bold'>Propagations</h2>{canCreate(user) && <Card><form action={createPropagationEvent} className='grid gap-3 md:grid-cols-2'><label className='grid gap-1 text-sm font-medium'>Method<select className='rounded-lg border px-3 py-2 font-normal' name='method'><option>LEAF</option><option>CUTTING</option><option>RHIZOME_SPLIT</option><option>DIVISION</option><option>SEED</option><option>TISSUE_CULTURE</option><option>RUNNER</option><option>OTHER</option></select></label><Field label='Date' name='date' type='date' required/><label className='grid gap-1 text-sm font-medium'>Parent / seed parent<select className='rounded-lg border px-3 py-2 font-normal' name='parent1'>{instances.map(i=><option key={i.id} value={i.id}>{i.plantId} · {plantName(i.plantDefinition)}</option>)}</select></label><label className='grid gap-1 text-sm font-medium'>Pollen parent, for seed only<select className='rounded-lg border px-3 py-2 font-normal' name='parent2'><option value=''>—</option>{instances.map(i=><option key={i.id} value={i.id}>{i.plantId} · {plantName(i.plantDefinition)}</option>)}</select></label><label className='grid gap-1 text-sm font-medium md:col-span-2'>Child plant IDs, one per line<textarea className='min-h-24 rounded-lg border px-3 py-2 font-normal' name='childCodes' required placeholder={'AV-001-P1\nAV-001-P2'} /></label><Field label='Child location' name='location'/><label className='grid gap-1 text-sm font-medium'>Success status<select className='rounded-lg border px-3 py-2 font-normal' name='successStatus'><option>PENDING</option><option>SUCCESS</option><option>PARTIAL</option><option>FAILED</option></select></label><label className='text-sm font-medium'><input type='checkbox' name='isSportCandidate'/> Children are sport candidates</label><label className='grid gap-1 text-sm font-medium'>Sport status<select className='rounded-lg border px-3 py-2 font-normal' name='sportStatus'><option>NONE</option><option>SUSPECTED</option><option>CANDIDATE</option><option>STABLE</option><option>UNSTABLE</option><option>REGISTERED</option></select></label><TextArea label='Notes' name='notes'/><TextArea label='Sport description' name='sportDescription'/><Button className='md:col-span-2'>Create propagation</Button></form></Card>}<div className='grid gap-3'>{events.map(e=><Card key={e.id}><div className='flex items-start justify-between gap-4'><div><h3 className='font-bold'>{fmtDate(e.date)} · {e.method} · {e.successStatus}</h3><p className='text-sm'>Parents: {e.parents.map(p=>`${p.parentRole}: ${p.parentPlantInstance.plantId}`).join(', ')}</p><p className='text-sm'>Children: {e.children.map(c=>c.childPlantInstance.plantId).join(', ')}</p><p className='text-sm'>{e.notes}</p></div>{isAdmin(user) && <div className='flex gap-2'><Link className='rounded-xl border px-3 py-2 text-sm' href={`/propagations/${e.id}/edit`}>Edit</Link><form action={deletePropagationEvent}><input type='hidden' name='id' value={e.id}/><ConfirmDeleteButton title='Delete propagation event?' message='This will permanently delete the propagation event and its parent/child links. Child plant instances will remain.'>Delete</ConfirmDeleteButton></form></div>}</div></Card>)}</div></div>
}
